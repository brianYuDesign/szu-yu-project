# Day 3 — AI 深度補強（4h）★ 本次衝刺的核心

**要解決的缺口**：Cake 唯一標為「工作經歷」層級的弱項 ——
> 「資料裡比較少看到您長期負責**模型評估、提示設計體系、資料標註策略或 AI 指標治理**這類更深的工作。」

**產出**：一份可展示的 AI 品質治理成果（eval set + 指標定義 + prompt 版本紀錄），以及履歷上三行新的、有東西撐的敘述。

---

## ★ 2026-07-31 更新：實際盤點後，Day 3 的性質變了

查過 `~/Project/feeling-bank/` 的程式碼後，**你的起點比原本估的高很多**。以下是實際狀況：

### 你已經有的（不用再做）

| 項目 | 證據 | 意義 |
|------|------|------|
| **AI 用量觀測層** | `bound-front-backend/models/aiusagelog.js`（`ai_usage_log` 表） | 已在記錄：`service`／`model`／`inputTokens`／`outputTokens`／`durationMs`／`success`／`errorCode`／`createdAt` |
| **輸出結構化** | `icebreakerService.js` 要求 JSON 格式輸出 | prompt injection 的輸出面已封 |
| **輸出端內容過濾** | 四組正則（風險／敏感／過度親暱／電話） | 不信任模型輸出的工程防線 |
| **三層 fallback** | AI → profile 模板 → 通用文案，合併去重 | 降級策略完整 |
| **雙層快取** | 記憶體 + Redis，key 帶 `v1` 版本前綴 | 成本控制 |
| **逾時依實測調整** | 4.5s → 8s，依 Sonnet 生產 P95 | 參數有數據支撐 |
| **AI 開發規範文件** | `sugarbee/.claude/skills/ai-service/SKILL.md` | 涵蓋選型、架構、成本護欄、輸出安全、政策紅線 |

**`ai_usage_log` 這張表是關鍵發現。** 它同時記了 token（成本）、`durationMs`（延遲）、`success`／`errorCode`（可靠性）—— 這代表**你不需要從零建觀測，資料已經在累積了**。

### 你還缺的（Day 3 要做的）

| 缺口 | 現況 |
|------|------|
| **品質評估** | ❌ 找不到任何 eval / golden set。`ai_usage_log` 記了成本、延遲、成功率，**但沒有任何欄位回答「輸出好不好」** |
| **資料標註** | ❌ 沒有 |
| **prompt 版本化** | ❌ prompt 是寫在 service 檔案裡的常數（如 `PREMIUM_ICEBREAKER_SYSTEM_PROMPT`），沒有 `prompts/` 目錄、沒有變更紀錄 |
| **日預算熔斷 / rate limit** | ❌ SKILL.md 有規範，但找不到實作 |
| **把既有資料變成指標** | ❌ 資料在表裡，但沒有產出過 P95、單次成本、失敗率這些數字 |

> **SKILL.md 第 7 節寫的「每個 AI 功能建 50–200 筆黃金評測集、改 prompt 必跑評測」是規範，不是既成事實。**
> 這件事很重要：**面試時不能講「我們有評測集」**，只能講「我們的規範要求建評測集」。差別很大，而且對方一問細節就會露餡。

---

## 所以 Day 3 你實際能獲得什麼

### 1. 唯一真正缺的那塊：品質評估

`ai_usage_log` 能告訴你「呼叫了幾次、多快、多貴、有沒有失敗」，但**回答不了「生成的破冰話題好不好」**。這正是 Cake 說的深度缺口，也是案例 A 目前唯一答不好的追問。

### 2. 把已有的資料變成可講的數字（投報率最高）

`ai_usage_log` 裡已經有資料，**寫一支查詢就能得到真實數字**，不用估算：

```sql
-- 各功能的 P95 延遲、平均 token、失敗率
SELECT service, model,
       COUNT(*) AS calls,
       AVG(inputTokens + outputTokens) AS avg_tokens,
       SUM(CASE WHEN success = 0 THEN 1 ELSE 0 END) / COUNT(*) AS failure_rate
FROM ai_usage_log
WHERE createdAt >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY service, model;
```

這些數字直接填進案例 A 的空欄（單次成本、P95 延遲、呼叫量），而且**是真實數據不是推估** —— 面試被追問時可以說「我查過」。

### 3. 履歷上完全沒提的一塊

目前履歷沒有任何一句提到 AI 可觀測性。但你有一張記錄 token、延遲、成功率的表 —— 這是「AI 指標治理」的具體證據。

### 4. 面試最痛那題的答案

> 「你怎麼確保模型輸出的品質？」

目前只能答「有正則過濾、有 fallback」—— 那是**防止最差**，不是**確保夠好**。做完評測集才有底氣回答。

---

## 修改後的工作量估計

原估 4 小時，**實際約 2.5–3 小時**，因為觀測層已存在：

| 任務 | 原估 | 修正 | 說明 |
|------|------|------|------|
| 建 eval set | 90 分 | **90 分** | 唯一沒得省的 —— 這是真正缺的東西 |
| prompt 版本管理 | 60 分 | **40 分** | prompt 已存在，只需抽出來加版號與 CHANGELOG |
| 定義三個指標 | 45 分 | **20 分** | 資料已在 `ai_usage_log`，只需寫查詢 |
| 回寫履歷 | 45 分 | **30 分** | 素材更多了 |

---

## 任務 1：建一組 eval set（90 分鐘）

挑**一個**功能就好，建議「破冰話題生成」或「個人簡介生成」（輸出短、好評分）。

### 步驟

1. **蒐集 30–50 個真實輸入**
   從產品現有資料抽（注意去識別化）。要涵蓋：典型案例、邊緣案例（資料極少的使用者）、惡意輸入（試圖越獄的 prompt）。

2. **定義「好」的標準**（這是最有價值的部分）
   例如破冰話題：
   | 維度 | 定義 | 評分 |
   |------|------|------|
   | 相關性 | 是否用到雙方共同興趣 | 0–2 |
   | 安全性 | 是否涉及敏感／冒犯內容 | 0/1（硬性） |
   | 可回覆性 | 對方能不能自然接話（非是非題） | 0–2 |
   | 語言自然度 | 是否像真人會說的話 | 0–2 |

3. **人工標一遍當基準線**
   30 筆自己標，記錄總分與各維度分數。這就是 Cake 說的「資料標註策略」。

4. **寫成可重跑的腳本**
   一支 Node/TS script：讀 eval set → 打模型 → 存結果 → 產出分數表。
   加分項：用 LLM-as-judge 做自動評分，但**必須先驗證 judge 和你的人工標註一致率**（算一下 agreement，低於 80% 就別用）。

### 產出檔案
```
eval/
  dataset.jsonl        # 輸入 + 人工標註
  rubric.md            # 評分標準定義
  run-eval.ts          # 執行腳本
  results/2026-07-31.json
```

---

## 任務 2：prompt 版本管理（60 分鐘）

現在你的 prompt 大概散在程式碼裡。改成有版本、有紀錄。

**最小可行做法**（不要引入 LangSmith 這類重工具，一人團隊用不上）：

1. prompt 抽成獨立檔案／常數，加上版本號與變更日期
2. 開一份 `prompts/CHANGELOG.md`：每次改 prompt 記下 **改了什麼 / 為什麼改 / eval 分數變化**
3. 把 eval 跑成 CI 的一個 job（或至少一個 npm script），**改 prompt 前後各跑一次**

這三步做完，你就能在面試回答：

> 「改 prompt 前後我會跑一組 40 筆的 eval set，看相關性和可回覆性的分數變化。上次調整 system prompt 讓相關性從 1.4 升到 1.7，但安全性掉了一筆，所以我又補了一條約束。」

這句話的份量，跟「我用 Claude 做了破冰話題」差三個級距。

---

## 任務 3：定義三個 AI 產品指標（45 分鐘）

Cake 說的「AI 指標治理」就是這個。挑三個你現在**就能量測**的：

| 類型 | 指標範例 | 為什麼重要 |
|------|---------|-----------|
| 品質 | eval 平均分、人工複審拒絕率 | 模型輸出有沒有變差 |
| 使用 | AI 功能採用率、生成內容的採納率（使用者真的送出了嗎） | AI 有沒有被用 |
| 成本／效能 | 單次呼叫成本、P95 延遲、快取命中率 | 能不能規模化 |

**採納率**特別重要 —— 它是「AI 功能有沒有真的產生價值」最直接的證據，而且多數候選人講不出來。

把這三個指標接到現有的觀測（你履歷有 Sentry / Grafana / Kibana），做一個最小 dashboard 或每週手動記錄表都行。

---

## 任務 4：回寫履歷（45 分鐘）

Day 1 改過的 `work.json` 感覺銀航段落，加上第 7 條：

> **AI 品質治理**：建立 40 筆標註 eval set 與四維度評分標準（相關性／安全性／可回覆性／自然度），將 prompt 變更納入版本管理與迴歸驗證；追蹤生成內容採納率、單次呼叫成本與 P95 延遲，作為功能調整依據。

`whyme.json` 的「AI 產品整合」擴寫成：

> **AI 產品整合與品質治理**：運用 Claude 實作配對建議、聊天教練與破冰話題／個人簡介生成，並整合至產品流程；建立提示設計的版本管理與評估機制，以標註資料集與量化指標驗證輸出品質，而非僅憑人工抽查。

**注意**：這兩段只有在任務 1–3 真的做完之後才能寫上去。面試官會追問細節，寫了做不到就是反效果。

---

## Day 3 驗收

- [ ] eval set 檔案存在，且至少跑過一次，有分數
- [ ] `prompts/CHANGELOG.md` 至少有一筆真實的變更紀錄（含分數前後對照）
- [ ] 三個指標定義清楚，且至少有一次實際量測值
- [ ] 能用 2 分鐘講完「我怎麼確保 AI 輸出品質」，全程不說「感覺」「大概」

---

## 若時間只夠做一件事

做**任務 1 的步驟 2（定義評分標準）+ 步驟 3（人工標 30 筆）**。

光是「我建了一組標註資料和評分標準」這件事，就已經跨過 Cake 指出的那道門檻 —— 大多數「會串 API」的工程師沒做過標註。
